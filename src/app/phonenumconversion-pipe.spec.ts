import { PhonenumconversionPipe } from './phonenumconversion-pipe';

describe('PhonenumconversionPipe', () => {
  it('create an instance', () => {
    const pipe = new PhonenumconversionPipe();
    expect(pipe).toBeTruthy();
  });
});
